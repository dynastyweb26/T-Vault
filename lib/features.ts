/**
 * Feature flags — flip to re-enable without restoring deleted code.
 * Voice Notes: hidden from UI and blocked at routes/API while implementation remains in repo.
 */
export const VOICE_NOTES_ENABLED = false;
